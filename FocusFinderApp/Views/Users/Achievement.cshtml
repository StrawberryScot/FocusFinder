@model FocusFinderApp.Models.Achievement

@{
    ViewData["Title"] = "Achievements";
}

<head>
    <link rel="stylesheet" href="~/css/home.css" asp-append-version="true" />
</head>

    @{
        if (ViewBag.IsLoggedIn == null || ViewBag.IsLoggedIn == false)
        {
        <div class="i-container">
            <div class="i-body-top body-top">
                <div class="text-center">
                    <h1 class="display-4">My Achievements</h1>
                </div>
            </div>
            <h5>Please login to view your achievements</h5>
            <a href="/Login" class="btn btn-primary">Go to Login</a>
        </div>
        }
        else
        {
        <div class="i-container">
        <h1>Achievements</h1>
        </div>
    <div class="i-container">
    <div class="achievement-category">
        <h2>Visits</h2>
        <div class="achievement @((Model.visits >= 1) ? "unlocked" : "locked")">
            <p>✅ Visited Your First Location</p>
            </div>
        <div class="achievement @((Model.visits >= 5) ? "unlocked" : "locked")">
            <p>🏅 Visited 5 Locations</p>
        </div>
        <div class="achievement @((Model.visits >= 10) ? "unlocked" : "locked")">
            <p>🏆 Visited 10 Locations</p>
        </div>
    </div>

    <div class="achievement-category">
        <h2>Cities Visited</h2>
        <div class="achievement @((Model.citiesVisited >= 1) ? "unlocked" : "locked")">
            <p>✅ Visited a New City</p>
        </div>
        <div class="achievement @((Model.citiesVisited >= 3) ? "unlocked" : "locked")">
            <p>🌍 Visited 3 Different Cities</p>
        </div>
    </div>

    <div class="achievement-category">
        <h2>Reviews</h2>
        <div class="achievement @((Model.reviewsLeft >= 1) ? "unlocked" : "locked")">
            <p>✅ Left Your First Review</p>
        </div>
        <div class="achievement @((Model.reviewsLeft >= 5) ? "unlocked" : "locked")">
            <p>💬 Left 5 Reviews</p>
        </div>
    </div>

    <div class="achievement-category">
        <h2>Bookmarks</h2>
        <div class="achievement @((Model.bookmarks >= 1) ? "unlocked" : "locked")">
            <p>✅ Bookmarked Your First Location</p>
        </div>
        <div class="achievement @((Model.bookmarks >= 5) ? "unlocked" : "locked")">
            <p>📌 Bookmarked 5 Locations</p>
        </div>
    </div>
    </div>
    <div class="filler"></div>
    }
    }



